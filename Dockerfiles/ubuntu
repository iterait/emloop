FROM ubuntu:yakkety
MAINTAINER Cognexa Solutions s.r.o. <docker@cognexa.com>

# copy the repository to the docker environment
COPY . cxflow
WORKDIR cxflow

# install dependencies
RUN apt -y update
RUN apt -y install git python3-dev python3-pip

# install cxflow
RUN pip3 install . && \
    python3 setup.py test
